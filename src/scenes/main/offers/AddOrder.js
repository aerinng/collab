import React from 'react';
import { View, StatusBar, Text } from "react-native";

export default AddOrder = ({ name }) => (
    <View style={{ flex: 1, backgroundColor: "#FFFFFF" }}>
        <StatusBar barStyle="light-content" />
        <Text>AddMeYes</Text>
    </View>
);